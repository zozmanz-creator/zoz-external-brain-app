const $=s=>document.querySelector(s),$$=s=>[...document.querySelectorAll(s)];
const state={venues:[],category:'all',district:'all',type:'all',music:'all',price:'all',openNow:false,kitchen:false,query:'',atmosphere:'all',favorites:new Set(JSON.parse(localStorage.getItem('zlachnyFavorites')||'[]')),map:null,markers:[]};
const accents={dance:'#caff00',talk:'#ff3d9d',date:'#ff6175',live:'#44d8ff',cinema:'#bd55ff',simple:'#ffcf35',weird:'#00ff9a',late:'#ff8a22',terrace:'#ff5aa9'};
const icon=id=>`<svg><use href="#${id}"/></svg>`;
function toast(text){const el=$('#toast');el.textContent=text;el.classList.add('show');clearTimeout(window.__toast);window.__toast=setTimeout(()=>el.classList.remove('show'),2400)}
function isOpenNow(v){if(!v.daysOpen?.includes(new Date().getDay()))return false;const now=new Date(new Date().toLocaleString('en-US',{timeZone:'Europe/Chisinau'}));const mins=now.getHours()*60+now.getMinutes();const parse=t=>{const [h,m]=t.split(':').map(Number);return h*60+m};const open=parse(v.openFrom),close=parse(v.openUntil);return close>open?mins>=open&&mins<close:mins>=open||mins<close}
function filteredVenues(){const q=state.query.trim().toLowerCase();return state.venues.filter(v=>(state.category==='all'||v.categories.includes(state.category))&&(state.district==='all'||v.district===state.district)&&(state.type==='all'||v.type===state.type)&&(state.music==='all'||v.music.includes(state.music))&&(state.price==='all'||v.priceLevel===state.price)&&(!state.openNow||isOpenNow(v))&&(!state.kitchen||v.kitchen)&&(!q||[v.name,v.type,v.district,v.address,v.description,...v.categories,...v.atmospheres,...v.tags,...v.music].join(' ').toLowerCase().includes(q))&&(state.atmosphere==='all'||v.atmospheres.includes(state.atmosphere)))}
function card(v){const key=v.categories[0]||'talk',color=accents[key]||'#ff3d9d',fav=state.favorites.has(v.id),open=isOpenNow(v);return `<article class="venue" data-venue="${v.id}" style="--accent:${color};--photo:url('${v.image}')"><div class="venue-photo"></div><div class="venue-top"><span class="tag">${v.type}${open?' · ОТКРЫТО':''}</span><button class="fav ${fav?'active':''}" data-fav="${v.id}" aria-label="Избранное">${icon('i-heart')}</button></div><div class="venue-content"><h3>${v.name}</h3><p>${v.description}</p><div class="venue-line">${icon('i-pin')}<span>${v.district} · ${v.hoursLabel}</span></div><div class="rating">${icon('i-star')}<b>${v.rating.toFixed(1)}</b><span>(${v.reviewsCount})</span></div><div class="meta">${v.tags.slice(0,3).map(t=>`<span>${t}</span>`).join('')}<b>${v.averageBillMDL} MDL</b></div></div></article>`}
function render(){const list=filteredVenues();$('#cards').classList.remove('loading');$('#cards').innerHTML=list.length?list.map(card).join(''):'<div class="empty">Ничего не найдено. Сбрось часть фильтров.</div>';$('#resultsCount').textContent=`Показано ${list.length} из ${state.venues.length}`;$('#mapCount').textContent=`${list.length} точек`;$$('[data-venue]').forEach(el=>el.addEventListener('click',()=>openVenue(el.dataset.venue)));$$('[data-fav]').forEach(btn=>btn.addEventListener('click',e=>{e.stopPropagation();const id=btn.dataset.fav;state.favorites.has(id)?state.favorites.delete(id):state.favorites.add(id);localStorage.setItem('zlachnyFavorites',JSON.stringify([...state.favorites]));render();toast(state.favorites.has(id)?'Добавлено в избранное':'Удалено из избранного')}));renderMap(list)}
function setCategory(value){state.category=value;$$('[data-category]').forEach(b=>b.classList.toggle('active',b.dataset.category===value));render()}
function openVenue(id){const v=state.venues.find(x=>x.id===id);if(!v)return;$('#modalPhoto').style.backgroundImage=`linear-gradient(180deg,transparent,rgba(0,0,0,.22)),url('${v.image}')`;$('#modalType').textContent=v.type;$('#modalName').textContent=v.name;$('#modalDescription').textContent=v.description;$('#modalDetails').innerHTML=[['Район',v.district],['Адрес',v.address],['Часы',v.hoursLabel],['Средний чек',`${v.averageBillMDL} MDL`],['Музыка',v.music.join(' · ')||'—'],['Кухня',v.kitchen?'Да':'Нет'],['Возраст',v.age],['Бронь',v.booking],['Телефон',v.phone||'—']].map(([a,b])=>`<div class="detail"><span>${a}</span><b>${b}</b></div>`).join('');setLink($('#modalMap'),v.mapUrl);setLink($('#modalWebsite'),v.website||v.instagram);$('#modalVerification').textContent=v.status==='verified'?`Проверено ${v.verifiedAt}. Источники: ${v.sources.join(', ')}`:'Демо-запись. Данные, ссылки и координаты будут опубликованы только после проверки источников.';$('#venueModal').classList.add('show')}
function setLink(el,url){if(url){el.href=url;el.classList.remove('disabled')}else{el.removeAttribute('href');el.classList.add('disabled')}}
function fillSelect(id,values,label){const el=$(id);el.innerHTML=`<option value="all">${label}</option>`+[...new Set(values)].sort((a,b)=>a.localeCompare(b,'ru')).map(x=>`<option value="${x}">${x}</option>`).join('')}
function buildCollections(){const defs=[['Где потанцевать в эти выходные',['dance']],['Коктейльные бары с атмосферой',['talk','date']],['Красивый вечер на террасе',['terrace','date']]];$('#collectionsList').innerHTML=defs.map(([title,cats])=>{const items=state.venues.filter(v=>cats.some(c=>v.categories.includes(c))).slice(0,4);const photo=items[0]?.image||'';return `<button class="collection-item" data-collection="${cats[0]}"><span class="collection-photo" style="background-image:url('${photo}')"></span><span><h3>${title}</h3><p>${items.map(x=>x.name).slice(0,2).join(' · ')}</p></span><span class="collection-count">${items.length} мест</span></button>`}).join('');$$('[data-collection]').forEach(b=>b.addEventListener('click',()=>{setCategory(b.dataset.collection);$('#places').scrollIntoView({behavior:'smooth'})}))}
function renderMap(list){if(window.L){if(!state.map){state.map=L.map('map',{zoomControl:true,attributionControl:false}).setView([47.0105,28.8638],13);L.tileLayer('https://{s}.tile.openstreetmap.org/{z}/{x}/{y}.png',{maxZoom:19}).addTo(state.map)}state.markers.forEach(m=>m.remove());state.markers=list.map(v=>{const marker=L.circleMarker([v.coordinates.lat,v.coordinates.lng],{radius:8,color:'#fff',weight:2,fillColor:accents[v.categories[0]]||'#ff3d9d',fillOpacity:1}).addTo(state.map);marker.bindPopup(`<b>${v.name}</b><br>${v.type}<br>${v.averageBillMDL} MDL`);marker.on('click',()=>openVenue(v.id));return marker});setTimeout(()=>state.map.invalidateSize(),50)}else{const box=$('#mapFallback');box.innerHTML=list.map(v=>{const x=20+((v.coordinates.lng-28.80)/.13)*60,y=80-((v.coordinates.lat-46.97)/.09)*60;return `<button class="fallback-pin" data-map-venue="${v.id}" style="left:${Math.max(6,Math.min(94,x))}%;top:${Math.max(7,Math.min(93,y))}%"></button>`}).join('');$$('[data-map-venue]').forEach(b=>b.addEventListener('click',()=>openVenue(b.dataset.mapVenue)))}}
function buildRoute(){let pool=filteredVenues();if(!pool.length)pool=state.venues;const budget=$('#routeBudget').value;if(budget!=='all')pool=pool.filter(v=>v.priceLevel===budget)||pool;const energy=$('#energy').value,company=$('#company').value;const pick=(cats,used=[])=>pool.find(v=>cats.some(c=>v.categories.includes(c))&&!used.includes(v.id))||pool.find(v=>!used.includes(v.id))||pool[0];const first=pick(energy==='calm'?['talk','date','terrace']:['talk','simple']);const second=pick(energy==='wild'?['dance','live']:energy==='calm'?['terrace','cinema','live']:['live','dance','terrace'],[first.id]);const third=pick(['late','simple'],[first.id,second.id]);const title=(energy==='wild'?'Сегодня без тормозов':energy==='calm'?'Красиво и без суеты':'Сначала культурно')+(company==='couple'?' · вдвоём':company==='solo'?' · соло':' · своей компанией');$('#routeResult').innerHTML=`<div class="route-title">Твой сценарий: «${title}»</div><div class="route-line">${[[first,'20:00 · РАЗОГРЕВ'],[second,'22:30 · ГЛАВНОЕ'],[third,'01:30 · ФИНАЛ']].map(([v,t])=>`<button class="route-stop" data-route-venue="${v.id}"><small>${t}</small><b>${v.name}</b><span>${v.type} · ${v.averageBillMDL} MDL</span></button>`).join('')}</div>`;$('#routeResult').classList.add('show');$$('[data-route-venue]').forEach(b=>b.addEventListener('click',()=>openVenue(b.dataset.routeVenue)))}
function reset(){Object.assign(state,{category:'all',district:'all',type:'all',music:'all',price:'all',openNow:false,kitchen:false,query:'',atmosphere:'all'});$$('select').forEach(s=>{if(['energy','company','routeBudget'].includes(s.id))return;s.value='all'});$('#searchInput').value='';$('#heroSearchInput').value='';$('#openNowFilter').checked=false;$('#kitchenFilter').checked=false;$$('[data-atmosphere]').forEach(b=>b.classList.remove('active'));setCategory('all')}
async function init(){try{const r=await fetch('venues.json?v=8',{cache:'no-store'});if(!r.ok)throw new Error('data');state.venues=(await r.json()).items||[];fillSelect('#districtFilter',state.venues.map(v=>v.district),'Все районы');fillSelect('#typeFilter',state.venues.map(v=>v.type),'Все типы');fillSelect('#musicFilter',state.venues.flatMap(v=>v.music),'Любая музыка');buildCollections();render()}catch(e){$('#cards').innerHTML='<div class="empty">Не удалось загрузить базу данных.</div>';$('#resultsCount').textContent='Ошибка загрузки'}
$$('[data-category]').forEach(b=>b.addEventListener('click',()=>setCategory(b.dataset.category)));[['#districtFilter','district'],['#typeFilter','type'],['#musicFilter','music'],['#priceFilter','price']].forEach(([id,key])=>$(id).addEventListener('change',e=>{state[key]=e.target.value;render()}));$('#openNowFilter').addEventListener('change',e=>{state.openNow=e.target.checked;render()});$('#kitchenFilter').addEventListener('change',e=>{state.kitchen=e.target.checked;render()});$('#searchInput').addEventListener('input',e=>{state.query=e.target.value;render()});$('#heroSearchForm').addEventListener('submit',e=>{e.preventDefault();state.query=$('#heroSearchInput').value;$('#searchInput').value=state.query;render();$('#places').scrollIntoView({behavior:'smooth'})});$('#resetFilters').addEventListener('click',reset);$$('[data-atmosphere]').forEach(b=>b.addEventListener('click',()=>{const value=b.dataset.atmosphere;state.atmosphere=state.atmosphere===value?'all':value;$$('[data-atmosphere]').forEach(x=>x.classList.toggle('active',x.dataset.atmosphere===state.atmosphere));render();$('#places').scrollIntoView({behavior:'smooth'})}));$$('[data-scroll]').forEach(b=>b.addEventListener('click',()=>$(b.dataset.scroll)?.scrollIntoView({behavior:'smooth'})));$('#buildRoute').addEventListener('click',buildRoute);$$('[data-close]').forEach(b=>b.addEventListener('click',()=>$('#'+b.dataset.close).classList.remove('show')));$('#venueModal').addEventListener('click',e=>{if(e.target===$('#venueModal'))$('#venueModal').classList.remove('show')});document.addEventListener('keydown',e=>{if(e.key==='Escape')$('#venueModal').classList.remove('show')});$('#newsletterForm').addEventListener('submit',e=>{e.preventDefault();e.currentTarget.reset();toast('Подписка будет подключена после запуска проверенной базы.')})}
document.addEventListener('DOMContentLoaded',init);
